# Manufacturing Co-Pilot Backend (FastAPI + LangChain)

## 🚀 Setup
```bash
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 10000
```

## 🌍 Environment
Create a `.env` file with your OpenAI key:
```
OPENAI_API_KEY=your_openai_api_key_here
```